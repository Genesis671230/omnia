import type { RamzaPage } from "./types";

/* Top-of-funnel explainers that earn topical authority and AI-search
   citations. Copy is hand-written per page — see types.ts. */

export const GUIDES: RamzaPage[] = [];
