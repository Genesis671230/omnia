import type { RamzaPage } from "./types";

/* Honest side-by-side pages against the alternatives a buyer is actually
   weighing. Every claim about another product carries a Citation with the date
   it was last checked. Copy is hand-written per page — see types.ts. */

export const COMPARISONS: RamzaPage[] = [];
